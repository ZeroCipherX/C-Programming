#include <stdio.h>
int sum(int, int);
int main(void)
{
    int x, y, z;
    for (int i = 0; i < 2; i++) {
        printf("Enter your numbers %d: ", i + 1);
        scanf("%d %d", &x, &y);
        sum =* ();
            }   
    printf("sum of x and y = %d", z);
    getchar(); 
}
int sum(int p, int q)
{
    int w;
    w = p + q;
    return w;
}